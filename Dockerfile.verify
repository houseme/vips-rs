# syntax=docker/dockerfile:1
FROM rust:1.88-bookworm

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        pkg-config \
        libvips-dev \
        libvips-tools \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace/vips-rs
