[package]
name = "vips"
version = "0.1.0"
authors = ["elbaro <elbaro@github>", "houseme <housemecn@gmail.com>"]
edition = "2024"
resolver = "3"
description = "Rust bindings for libvips: fast, low-memory image processing with a safe, ergonomic API."
keywords = ["vips", "image", "libvips"]
documentation = "https://houseme.github.io/vips-rs/vips/"
repository = "https://github.com/houseme/vips-rs"
readme = "README.md"
categories = ["multimedia::images"]
license = "MIT"

# Published builds resolve vips-sys from crates.io. Use [patch.crates-io]
# with a sibling path when developing against a local vips-sys checkout.
[dependencies]
vips-sys = { version = "0.2.0" }

[dev-dependencies]
trybuild = "1.0"

[package.metadata.docs.rs]
all-features = true
default-target = "x86_64-unknown-linux-gnu"
